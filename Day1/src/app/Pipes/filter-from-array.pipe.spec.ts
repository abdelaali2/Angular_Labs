import { FilterFromArrayPipe } from './filter-from-array.pipe';

describe('FilterFromArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterFromArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
